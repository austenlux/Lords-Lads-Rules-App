<resources>
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:textColor">@android:color/black</item>
    <item name="android:editTextStyle">@style/ResetEditText</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="android:windowBackground">@color/splashscreen_background</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
    <item name="android:enforceNavigationBarContrast">false</item>
  </style>
  <style name="ResetEditText" parent="@android:style/Widget.EditText">
    <item name="android:padding">0dp</item>
    <item name="android:textColorHint">#c8c8c8</item>
    <item name="android:textColor">@android:color/black</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
    <!-- Dark background — matches the RN splash overlay so there is no flash -->
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
    <!-- Invisible icon: same color as background so the mandatory Android 12+
         icon area is not visible; the RN overlay is the only logo shown -->
    <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_bg</item>
    <!-- Switch to AppTheme the instant the system splash exits -->
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>
</resources>
